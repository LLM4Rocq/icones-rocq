% In this file you should put macros to be used only by the web
% version. Each one should have a corresponding version in
% macros/print.tex. Typically the web version could have more fancy
% decorations than the print version.
% This will probably be a very short file.
