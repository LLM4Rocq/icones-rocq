# Rocq build artefacts
*.vo
*.vok
*.vos
*.glob
*.aux
.*.aux

# Coq/Rocq make scaffolding
Makefile.coq
Makefile.coq.conf
.Makefile.coq.d
.lia.cache
.nia.cache
.coq-native/

# rocq-mcp cache
rocq_mcp_cache_*.v

# Claude Code local lock files (per-session)
.claude/

# Python venv
.venv/

# Editor / OS
*~
\#*\#
.DS_Store
.vscode/
.idea/

# Local scratch
scratch/
*.bak

# Paper (not tracked — local-only artifact)
paper/

# Auditor build
tools/**/__pycache__/
tools/**/*.pyc
# The rendered dashboard: CI assembles it into site/ and uploads it as a
# Pages artefact, and the README's local-build recipe writes it here too.
site/
# Tarball of theories/**/*.glob — CI ships the real dependency-graph data
# from build.yml to the docs workflow's build-auditor through it; reproducing
# that packaging step locally must not leave a tracked artefact behind.
theories-glob.tar.gz

# Modern Rocq (post 9.0) make scaffolding
Makefile.conf
.Makefile.d
